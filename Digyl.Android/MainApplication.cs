using System;
/*using Android.App;
using Android.Runtime;
using Shiny;

namespace Digyl.Droid
{
    [Application]
    public class MainApplication : Shiny.ShinyAndroidApplication<Digyl.MyStartup>
    {
        public MainApplication(IntPtr handle, JniHandleOwnership transfer) : base(handle, transfer)
        {
        }

        public override void OnCreate()
        {
            base.OnCreate();
            //AndroidShinyHost.Init(this, new MyStartup(), platformBuild: services => services.UseNotifications());
        }
    }
}
*/